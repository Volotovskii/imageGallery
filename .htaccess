RewriteEngine On

# Если запрашиваемый файл или папка существует — отдать напрямую
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Перенаправить всё в index.php
RewriteRule ^(.*)$ index.php [QSA,L]